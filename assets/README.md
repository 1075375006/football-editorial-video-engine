# Optional assets

Place user-owned or explicitly licensed environment and subject media in the two subdirectories. The contents are ignored by default so a public clone stays reproducible and does not accidentally publish private or unlicensed media. The offline demo intentionally uses programmatic fallbacks.
